URL:
    Protocolo: http
    ://
    Dominio: www.midominio.com
    /
    Ruta: carpeta/index.php

    Parametro de busqueda: coches.com?q=ferrari
    // Los formularios se suelen enviar por "post", porque no se muestra en la URL, cosa que sí ocurre con "get"
        Parámetro: q || id
        Valor del parámetro: ferrari || 5


Envio de formulario a un programa:
    <form action="index.php" method="get || post">
        <input type="text" name="key"/>
        <button type="submit">Click</button>
    </from>
    // De forma nativa la URL se mostrará  en el siguiente formato: .../mibanco/micarpeta/index.php?key=Juan555 (Si usamos GET)

      CLIENTE               SERVIDOR
     _________    quest     _________
    |         | ---------> |         |
    |         |            |         |
    |         | <--------- |         |
    |         |  request   |         |
     _________              _________
     navegador              php || Java
                            C# || Python

    // Jee (Java Enterprise Edition): jsp(Java Sever Page), servlets, ejb (Enterprise Java Bean)

    MVC:
        Vista (View)    |   Lógica (Controlador -C- && Modelo -M-)
        Interfaz        |   Lógica
        (visual)        |   (código programación)

    Modelo: conexión a la base de datos
    Controlador: llama al Modelo y este le devuelve los datos, de forma que el Controlador llama a la Vista para hacer el request.